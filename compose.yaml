version: '3'
services:
  web:
    build: .
    ports:
      - '4173:4173'
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - external-network
networks:
  external-network:
    internal: false
